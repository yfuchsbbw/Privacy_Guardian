<UserControl x:Class="PrivacyGuardian.Views.FirewallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:core="clr-namespace:PrivacyGuardian.Core">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" Visibility="Collapsed" />
        <Border Grid.Row="1" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,16,0" />
                <TextBox Grid.Column="1" Text="{Binding NewRuleName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                <TextBox Grid.Column="2" Text="{Binding NewProgramPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                <Button Grid.Column="3" Content="{Binding CreateBlockRuleText}" Command="{Binding CreateRuleCommand}" />
                <Button Grid.Column="4" Content="{Binding RefreshText}" Command="{Binding RefreshCommand}" />
            </Grid>
        </Border>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Rules}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="110" />
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="110" />
                <DataGridTextColumn Header="Program" Binding="{Binding Program}" Width="3*" />
                <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding Enabled}" Width="90" />
                <DataGridTemplateColumn Header="Remove" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{Binding DataContext.RemoveText, RelativeSource={RelativeSource AncestorType=DataGrid}}" Command="{Binding DataContext.RemoveRuleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
